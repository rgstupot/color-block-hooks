import React from 'react';

const Instructions = () => {
    return (
        <div className="d-flex justify-content-center m-3">
            <h4>Add some color to this box! Click on the corresponding button to change the color of the box!</h4>
        </div>
    );
}

export default Instructions;